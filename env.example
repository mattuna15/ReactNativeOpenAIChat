```env
# Required: your OpenAI API key (keep this secret)
OPENAI_API_KEY=sk-your-actual-api-key-here

# Optional: enable debug logging in the app (true/1/yes)
# If not present, the app will fall back to React Native's __DEV__ value
DEBUG=true

# SECURITY: Do NOT commit your real key. Treat the value above as an example.
```
OPENAI_API_KEY=sk-your-actual-api-key-here
